# platform 目录结构说明

本目录放平台层能力：配置与能力注册，不直接承载业务流程。

## 文件说明

- `config.py`：环境变量与 `.env` 配置加载（`Config`）。
- `mcp.py`：MCP 能力到工具的映射注册（`MCPRegistry`）。
- `__init__.py`：platform 层导出入口。

## 典型改动

- 新增环境变量配置项。
- 增加新的 MCP capability 与工具映射。
